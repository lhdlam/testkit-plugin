---
name: "testkit:scenarios"
description: Phase 3 — validate selectors against the real UI (ui-map.md) and assemble end-to-end scenarios (scenarios.md). Gated by test-cases.md APPROVED.
category: Testing
tags: [testkit, scenarios, ui-map]
---

Invoke the `map-and-scenario` skill using the Skill tool. Pass through any arguments.

Pre-flight: requires `test-cases.md` `> Review: APPROVED`. Produces `ui-map.md` (+ `untestable.md`)
and `scenarios.md`, ending with `> Review: PENDING`.
